from StudentRecord import StudentRecord
import sys

val = sys.argv[len(sys.argv) - 1]

def main():
 with open(val, "r") as ins:
array = []
n = 0
sum = 0
student.input(line)
array.append(student)
n = n+1
sum = sum + float(student.score())
average = sum/n
print (average)
